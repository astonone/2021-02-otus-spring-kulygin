app:
  locale: en

spring:
  messages:
    basename: i18n/messages
  datasource:
    url: jdbc:h2:mem:library
  liquibase:
    enabled: true
    change-log: db/changelog/db.changelog-master-test.yaml
  shell:
    interactive:
      enabled: false